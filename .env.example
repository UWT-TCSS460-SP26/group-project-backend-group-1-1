PORT=3000
API-KEY=your_tmdb_read_access_token_here
DATABASE_URL="postgresql://tcss460:tcss460dev@localhost:5433/tcss460?schema=public"

# Auth² (Sprint 3)
AUTH_ISSUER=https://tcss-460-iam.onrender.com
API_AUDIENCE=group-1-api

# CORS Configuration
# Comma-separated list of allowed origins. 
# Defaults to localhost:3000, 5173, 4173 in dev if empty.
# Set to * to allow all (not recommended for production).
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
