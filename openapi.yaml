openapi: 3.0.3
info:
  title: TCSS 460 Group Project API
  description: |
    **Team:** Nate, Harleen, Harsimar, Jonathan

    **Description:** A RESTful API for the TCSS 460 group project, proxying TMDB data for movies and TV shows through team-designed endpoints.

    **Deployed URL:** https://tcss460-team-1-api.onrender.com
  version: 0.1.0

servers:
  - url: https://tcss460-team-1-api.onrender.com
    description: Production
  - url: http://localhost:3000
    description: Local development

tags:
  - name: Health
    description: Health check endpoints
  - name: Movies
    description: Movie search, popular, and detail endpoints
  - name: TV
    description: TV show search, popular, and detail endpoints
  - name: Ratings
    description: User-submitted numeric ratings for movies and TV shows
  - name: Reviews
    description: User-submitted text reviews for movies and TV shows

paths:
  /health:
    get:
      tags: [Health]
      summary: Health Check
      description: Returns the health status of the API.
      responses:
        '200':
          description: API is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: 'GOOD'

  /movies/popular:
    get:
      tags: [Movies]
      summary: Popular Movies
      description: |
        Returns the most popular movies, sorted by popularity descending.
        Proxies TMDB's `/discover/movie` endpoint.
      parameters:
        - name: language
          in: query
          required: false
          description: BCP-47 language tag (e.g. `en-US`, `es-ES`, `ja-JP`).
          schema:
            type: string
            default: en-US
            example: en-US
        - name: page
          in: query
          required: false
          description: 1-based page number.
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        '200':
          description: Paginated list of popular movies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PopularMoviesResponse'
        '500':
          description: TMDB API key not configured on the server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Upstream TMDB unreachable or returned an invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /movies/search:
    get:
      tags: [Movies]
      summary: Search Movies
      description: |
        Searches for movies by title. Requires the `query` parameter.
        Proxies TMDB's `/search/movie` endpoint.
      parameters:
        - name: query
          in: query
          required: true
          description: Title to search for.
          schema:
            type: string
            example: batman
        - name: language
          in: query
          required: false
          description: BCP-47 language tag (e.g. `en-US`, `es-ES`, `ja-JP`).
          schema:
            type: string
            default: en-US
            example: en-US
        - name: page
          in: query
          required: false
          description: 1-based page number.
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        '200':
          description: Search results for movies.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovieSearchResponse'
        '400':
          description: Query parameter is missing or empty.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: TMDB API key not configured on the server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Upstream TMDB unreachable or returned an invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /movies/{id}:
    get:
      tags: [Movies]
      summary: Movie Details
      description: |
        Returns detailed information for a specific movie by TMDB ID.
        Proxies TMDB's `/movie/{id}` endpoint.
      parameters:
        - name: id
          in: path
          required: true
          description: TMDB movie ID.
          schema:
            type: integer
            example: 550
        - name: language
          in: query
          required: false
          description: BCP-47 language tag (e.g. `en-US`, `es-ES`, `ja-JP`).
          schema:
            type: string
            default: en-US
            example: en-US
      responses:
        '200':
          description: Detailed movie information.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MovieDetailsResponse'
        '400':
          description: Movie ID is missing or invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Movie not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: TMDB API key not configured on the server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Upstream TMDB unreachable or returned an invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /tv/popular:
    get:
      tags: [TV]
      summary: Popular TV Shows
      description: |
        Returns the most popular TV shows, sorted by popularity descending.
        Proxies TMDB's `/discover/tv` endpoint.
      parameters:
        - name: language
          in: query
          required: false
          description: BCP-47 language tag (e.g. `en-US`, `es-ES`, `ja-JP`).
          schema:
            type: string
            default: en-US
            example: en-US
        - name: page
          in: query
          required: false
          description: 1-based page number.
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        '200':
          description: Paginated list of popular TV shows.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PopularTvResponse'
        '500':
          description: TMDB API key not configured on the server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Upstream TMDB unreachable or returned an invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /tv/search:
    get:
      tags: [TV]
      summary: Search TV Shows
      description: |
        Searches for TV shows by title. Requires the `query` parameter.
        Proxies TMDB's `/search/tv` endpoint.
      parameters:
        - name: query
          in: query
          required: true
          description: Title to search for.
          schema:
            type: string
            example: game of thrones
        - name: language
          in: query
          required: false
          description: BCP-47 language tag (e.g. `en-US`, `es-ES`, `ja-JP`).
          schema:
            type: string
            default: en-US
            example: en-US
        - name: page
          in: query
          required: false
          description: 1-based page number.
          schema:
            type: integer
            minimum: 1
            default: 1
      responses:
        '200':
          description: Search results for TV shows.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TvSearchResponse'
        '400':
          description: Query parameter is missing or empty.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: TMDB API key not configured on the server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Upstream TMDB unreachable or returned an invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /tv/{id}:
    get:
      tags: [TV]
      summary: TV Show Details
      description: |
        Returns detailed information for a specific TV show by TMDB ID.
        Proxies TMDB's `/tv/{id}` endpoint.
      parameters:
        - name: id
          in: path
          required: true
          description: TMDB TV show ID.
          schema:
            type: integer
            example: 1399
        - name: language
          in: query
          required: false
          description: BCP-47 language tag (e.g. `en-US`, `es-ES`, `ja-JP`).
          schema:
            type: string
            default: en-US
            example: en-US
      responses:
        '200':
          description: Detailed TV show information.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TvDetailsResponse'
        '400':
          description: TV show ID is missing or invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: TV show not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: TMDB API key not configured on the server.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '502':
          description: Upstream TMDB unreachable or returned an invalid body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /ratings:
    post:
      tags: [Ratings]
      summary: Create Rating
      description: Creates a numeric rating (0–10) for a movie or TV show, owned by the authenticated user.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RatingInput'
      responses:
        '201':
          description: Rating created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Rating'
        '400':
          description: Invalid rating fields or score out of range.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /ratings/{id}:
    get:
      tags: [Ratings]
      summary: Get Rating by ID
      description: Returns a single rating by its primary key.
      parameters:
        - name: id
          in: path
          required: true
          description: Rating primary key.
          schema:
            type: integer
            example: 1
      responses:
        '200':
          description: The requested rating.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Rating'
        '400':
          description: Invalid rating id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Rating not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    put:
      tags: [Ratings]
      summary: Update Rating
      description: Updates the score of a rating owned by the authenticated user.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                score:
                  type: integer
                  minimum: 0
                  maximum: 10
                  example: 9
              required: [score]
      responses:
        '200':
          description: Updated rating.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Rating'
        '400':
          description: Invalid id, invalid fields, or score out of range.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Caller does not own this rating.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Rating not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      tags: [Ratings]
      summary: Delete Rating
      description: Deletes a rating owned by the authenticated user.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            example: 1
      responses:
        '204':
          description: Rating deleted.
        '400':
          description: Invalid rating id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Caller does not own this rating.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Rating not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /ratings/media/{mediaType}/{tmdbId}:
    get:
      tags: [Ratings]
      summary: List Ratings for a Media Item
      description: Returns paginated ratings plus an aggregate average for a given TMDB media item.
      parameters:
        - name: mediaType
          in: path
          required: true
          schema:
            type: string
            enum: [movie, tv]
            example: tv
        - name: tmdbId
          in: path
          required: true
          schema:
            type: string
            example: '1399'
        - name: page
          in: query
          required: false
          schema:
            type: integer
            default: 1
            minimum: 1
        - name: limit
          in: query
          required: false
          description: Page size (capped at 50).
          schema:
            type: integer
            default: 10
            minimum: 1
            maximum: 50
      responses:
        '200':
          description: Page of ratings with aggregate average.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingsListResponse'
        '400':
          description: Invalid mediaType or tmdbId.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /reviews:
    post:
      tags: [Reviews]
      summary: Create Review
      description: Creates a text review for a movie or TV show, owned by the authenticated user.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReviewInput'
      responses:
        '201':
          description: Review created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Review'
        '400':
          description: Invalid review fields.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Missing or invalid bearer token.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /reviews/{id}:
    get:
      tags: [Reviews]
      summary: Get Review by ID
      description: Returns a single review by its primary key.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            example: 1
      responses:
        '200':
          description: The requested review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Review'
        '400':
          description: Invalid review id.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Review not found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '500':
          description: Database error.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Bearer token issued by `POST /auth/dev-login` (dev) or the eventual auth provider.
        Send as `Authorization: Bearer <token>`.
  schemas:
    ErrorResponse:
      type: object
      description: Standard error envelope returned on 4xx and 5xx responses.
      properties:
        message:
          type: string
          description: Human-readable description of the error.
          example: 'Missing required query parameter: query'
      required: [message]

    PopularMovieSummary:
      type: object
      description: |
        Trimmed movie shape returned by `GET /movies/popular`.
        Maps upstream TMDB field names to our public names.
      properties:
        id:
          type: integer
          description: TMDB movie ID.
          example: 640146
        title:
          type: string
          description: Movie title (mapped from upstream `original_title`).
          example: 'Ant-Man and the Wasp: Quantumania'
        overview:
          type: string
          description: Short plot synopsis.
          example: 'Super-Hero partners Scott Lang and Hope van Dyne...'
        poster_path:
          type: string
          nullable: true
          description: Relative poster image path. Prepend `https://image.tmdb.org/t/p/w500` for a full URL.
          example: '/ngl2FKBlU4fhbdsrtdom9LVLBXw.jpg'
        release_date:
          type: string
          format: date
          description: Theatrical release date (ISO 8601).
          example: '2023-02-15'
        language:
          type: string
          description: Original language code (mapped from upstream `original_language`).
          example: en
      required: [id, title, overview, poster_path, release_date, language]

    PopularMoviesResponse:
      type: object
      description: Envelope returned by `GET /movies/popular`.
      properties:
        language:
          type: string
          description: The BCP-47 language tag that was used for the upstream request.
          example: en-US
        page:
          type: integer
          description: Current page number.
          example: 1
        results:
          type: array
          items:
            $ref: '#/components/schemas/PopularMovieSummary'
      required: [language, page, results]

    MovieSearchResult:
      type: object
      description: Summary movie shape returned by `GET /movies/search`.
      properties:
        id:
          type: integer
          example: 550
        title:
          type: string
          example: 'Fight Club'
        overview:
          type: string
          example: 'An insomniac office worker and a devil-may-care soap maker...'
        poster_path:
          type: string
          nullable: true
          example: '/i8M6qh39uJVvP9w5ub-h0qMjFk5.jpg'
        release_date:
          type: string
          format: date
          example: '1999-10-15'
        language:
          type: string
          example: en
      required: [id, title, overview, poster_path, release_date, language]

    MovieSearchResponse:
      type: object
      description: Envelope returned by `GET /movies/search`.
      properties:
        query:
          type: string
          example: fight
        language:
          type: string
          example: en-US
        page:
          type: integer
          example: 1
        results:
          type: array
          items:
            $ref: '#/components/schemas/MovieSearchResult'
      required: [query, language, page, results]

    PopularTvSummary:
      type: object
      description: Trimmed TV show shape returned by `GET /tv/popular`.
      properties:
        id:
          type: integer
          example: 1399
        title:
          type: string
          description: TV show name (mapped from upstream `original_name`).
          example: 'Game of Thrones'
        overview:
          type: string
          example: 'Seven noble families fight for control of the lands of Westeros...'
        poster_path:
          type: string
          nullable: true
          description: Relative poster image path. Prepend `https://image.tmdb.org/t/p/w500` for a full URL.
          example: '/u3bZgnGQ9T01sWNhyveQz0wH0Hl.jpg'
        first_air_date:
          type: string
          format: date
          example: '2011-04-17'
        language:
          type: string
          description: Original language code (mapped from upstream `original_language`).
          example: en
      required: [id, title, overview, poster_path, first_air_date, language]

    PopularTvResponse:
      type: object
      description: Envelope returned by `GET /tv/popular`.
      properties:
        language:
          type: string
          description: The BCP-47 language tag used for the upstream request.
          example: en-US
        page:
          type: integer
          example: 1
        results:
          type: array
          items:
            $ref: '#/components/schemas/PopularTvSummary'
      required: [language, page, results]

    TvSearchResult:
      type: object
      description: Summary TV show shape returned by `GET /tv/search`.
      properties:
        id:
          type: integer
          example: 1399
        title:
          type: string
          example: 'Game of Thrones'
        overview:
          type: string
          example: 'Seven noble families fight for control of the mythical land of Westeros.'
        posterPath:
          type: string
          nullable: true
          example: '/tv-poster.jpg'
        firstAirDate:
          type: string
          format: date
          example: '2011-04-17'
        year:
          type: integer
          nullable: true
          example: 2011
        originalLanguage:
          type: string
          example: en
      required: [id, title, overview, posterPath, firstAirDate, year, originalLanguage]

    TvSearchResponse:
      type: object
      description: Envelope returned by `GET /tv/search`.
      properties:
        query:
          type: string
          example: throne
        language:
          type: string
          example: en-US
        page:
          type: integer
          example: 1
        results:
          type: array
          items:
            $ref: '#/components/schemas/TvSearchResult'
      required: [query, language, page, results]

    MovieDetailsResponse:
      type: object
      description: Detailed movie information returned by `GET /movies/{id}`.
      properties:
        id:
          type: integer
          example: 550
        title:
          type: string
          example: 'Fight Club'
        overview:
          type: string
          example: 'A ticking-time-bomb insomniac and a slippery soap salesman...'
        poster_path:
          type: string
          nullable: true
          example: '/jSziioSwPVrOy9Yow3XhWIBDjq1.jpg'
        backdrop_path:
          type: string
          nullable: true
          example: '/c6OLXfKAk5BKeR6broC8pYiCquX.jpg'
        release_date:
          type: string
          format: date
          example: '1999-10-15'
        language:
          type: string
          example: en
        rating:
          type: number
          format: float
          example: 8.4
        runtime:
          type: integer
          example: 139
        genres:
          type: array
          items:
            type: string
          example: ['Drama', 'Thriller']
      required:
        [
          id,
          title,
          overview,
          poster_path,
          backdrop_path,
          release_date,
          language,
          rating,
          runtime,
          genres,
        ]

    TvDetailsResponse:
      type: object
      description: Detailed TV show information returned by `GET /tv/{id}`.
      properties:
        id:
          type: integer
          example: 1399
        title:
          type: string
          example: 'Game of Thrones'
        overview:
          type: string
          example: 'Seven noble families fight for control of the mythical land of Westeros.'
        poster_path:
          type: string
          nullable: true
          example: '/tv-poster.jpg'
        backdrop_path:
          type: string
          nullable: true
          example: '/tv-backdrop.jpg'
        first_air_date:
          type: string
          format: date
          example: '2011-04-17'
        language:
          type: string
          example: en
        rating:
          type: number
          format: float
          example: 8.5
        number_of_seasons:
          type: integer
          example: 8
        number_of_episodes:
          type: integer
          example: 73
        status:
          type: string
          example: 'Ended'
        genres:
          type: array
          items:
            type: string
          example: ['Sci-Fi & Fantasy']
      required:
        [
          id,
          title,
          overview,
          poster_path,
          backdrop_path,
          first_air_date,
          language,
          rating,
          number_of_seasons,
          number_of_episodes,
          status,
          genres,
        ]

    Rating:
      type: object
      description: A user-submitted numeric rating for a TMDB media item.
      properties:
        id:
          type: integer
          example: 1
        userId:
          type: integer
          description: ID of the user who owns this rating.
          example: 1
        score:
          type: integer
          minimum: 0
          maximum: 10
          example: 8
        tmdbId:
          type: string
          description: TMDB identifier for the rated media item.
          example: '1399'
        mediaType:
          type: string
          enum: [movie, tv]
          example: tv
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, userId, score, tmdbId, mediaType]

    RatingInput:
      type: object
      description: Request body for `POST /ratings`.
      properties:
        tmdbId:
          type: string
          example: '1399'
        mediaType:
          type: string
          enum: [movie, tv]
          example: tv
        score:
          type: integer
          minimum: 0
          maximum: 10
          example: 8
      required: [tmdbId, mediaType, score]

    RatingsListResponse:
      type: object
      description: Paginated ratings list with aggregate average.
      properties:
        page:
          type: integer
          example: 1
        limit:
          type: integer
          example: 10
        total:
          type: integer
          description: Total ratings for this media item across all pages.
          example: 1
        averageScore:
          type: number
          format: float
          description: Average score across all ratings for this media item. `0` when there are no ratings.
          example: 8
        results:
          type: array
          items:
            $ref: '#/components/schemas/Rating'
      required: [page, limit, total, averageScore, results]

    Review:
      type: object
      description: A user-submitted text review for a TMDB media item.
      properties:
        id:
          type: integer
          example: 1
        userId:
          type: integer
          example: 1
        title:
          type: string
          example: 'Brilliant'
        description:
          type: string
          example: 'A masterpiece of modern cinema.'
        tmdbId:
          type: string
          example: '1399'
        mediaType:
          type: string
          enum: [movie, tv]
          example: tv
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, userId, title, description, tmdbId, mediaType]

    ReviewInput:
      type: object
      description: Request body for `POST /reviews`.
      properties:
        tmdbId:
          type: string
          example: '1399'
        mediaType:
          type: string
          enum: [movie, tv]
          example: tv
        title:
          type: string
          example: 'Brilliant'
        description:
          type: string
          example: 'A masterpiece of modern cinema.'
      required: [tmdbId, mediaType, title, description]
